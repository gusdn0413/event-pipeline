# Prometheus 메트릭 저장용 영속 디스크
# Pod가 재시작되어도 수집된 시계열 데이터 유지
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-data
spec:
  accessModes: ["ReadWriteOnce"]   # 한 노드에서 한 Pod만 마운트 (Prometheus 1대 운영)
  resources:
    requests:
      storage: 5Gi                 # 14일치 메트릭 보관 기준
